{
  "name": "human-resources-management-mcp",
  "version": "1.0.0",
  "description": "MCP Server for Human Resources Management - Streamable HTTP + Stdio transport",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "start:http": "node dist/main.js --http",
    "start:stdio": "node dist/main.js --stdio"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}